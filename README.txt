Install Python
Run command in terminal: python jupyter notebook
Browser will open with Jupyter Notebook Instance
Navigate to the folder where codes are place
Open the code
Execute Run All (Play button to the top left)